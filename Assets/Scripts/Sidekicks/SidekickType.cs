namespace Game.Sidekicks
{
    public enum SidekickType
    {
        Damage,   // adds % of hero ATK each strike
        Heal,     // heals % maxHP on each hero beat
        Block,    // reduces incoming damage %
        Crit      // adds crit chance
    }
}
